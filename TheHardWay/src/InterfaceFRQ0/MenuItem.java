package InterfaceFRQ0;

public interface MenuItem {

	//The method below is public and abstract by default,
	//because it is in an interface.
	String getName();
	
	//The method below is public and abstract by default,
	//because it is in an interface.
	double getPrice();
}
